# 变更记录

## 2026-04-14

- 将项目新增为网页版形态，前端改为 HTML，后端新增 Node.js 服务
- 新增本地 JSON 存储、任务 API、活动流 API、候选任务与日程摘要接口
- 重写 `index.html`，改为真实调用后端接口的管理台页面
- 将后端源码入口与服务统一收拢到 `backend/` 目录
- 新增任务编辑、搜索筛选、切换完成、清理已完成、快速记录、数据导入导出和拖拽截图上传
- 启动脚本切换为 `pnpm install` + `pnpm start`
- 在 `test/` 下新增 Node.js 自测，覆盖任务服务、摘要、候选任务与截图分析逻辑
- 将项目产品定位升级为“像素风小猫桌宠 + 日程整理助手”
- 参考 BongoCat 重做桌面主界面，改为像素猫主体视觉
- 参考 screenpipe 的思路，新增本地活动流记录层
- 新增前台窗口持续采集，形成轻量时间轴上下文
- 新增低频屏幕快照时间轴，支持缩略图预览与再次识别
- 新增历史快照选择浏览与任意快照识别
- 新增活动流候选任务整理与一键采纳
- 新增今日日程整理摘要，根据任务池和活动流生成建议安排
- 为新增的活动流管理器、日程整理器、窗口采集器、快照管理器和候选任务整理器补充基础测试
- 前端新增“流光星空”鼠标交互粒子网络背景，将主题色调由明亮极简切换为暗夜极简（玻璃拟态面板+青蓝流光特效），大幅提升视觉互动体验
- 新增 `config.js` 运行时配置，支持将 `demo.html` 的接口地址切换到 Render 后端
- 调整 `vercel.json` 为 `cleanUrls`，避免将全部路径错误重写到首页导致体验页与 API 链路异常
- 更新 README 云端部署说明，补充 Render 一体部署与 Vercel + Render 分离部署步骤
- 新增 `vercel-app/` 纯静态前端目录，用于在 Vercel 以独立根目录方式部署首页与体验页
